- if @users_drive_way.errors.any?
  #error_explanation
    %h2= "#{pluralize(@users_drive_way.errors.count, "error")} prohibited this users_drive_way from being saved:"
    %ul
      - @users_drive_way.errors.full_messages.each do |msg|
        %li= msg

.field
  = f.label :price
  = f.text_field :price
.field
  = f.label :name
  = f.text_field :name
.field
  = f.label :description
  = f.text_field :description
.actions
  = f.submit 'Save'
